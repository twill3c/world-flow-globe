# SPEC.md — world-flow-globe(WORLD FLOW GLOBE V3.2)

<!-- scaffold template v1.30.0 から展開(2026-09-07)。以後このファイルはプロジェクトが育てる -->

原典は利用者から会話中に受領した「WORLD FLOW GLOBE V3.2 完全実装仕様書」。収録しない理由と節番号の対応表は `docs/ORIGIN.md` にある。
本ファイルは、その仕様を**この機・この出典・この予算で実際に作れる形**へ落としたものである。
原典と食い違う箇所は §9「原典との差分」にすべて列挙する。**黙って変えた箇所は無い。**

---

## 1. 目的

世界の港・海洋境界・推定航路・チョークポイントを 3D 地球儀の上に**独立したレイヤー**として重ね、
「スエズを 7 日閉じたら、どの航路がどれだけ遠回りになり、どの港に荷が寄るか」を
ブラウザの中だけで決定論的に計算して見せる。

原典 §151 の最重要メッセージ —— **「世界のデータを地球儀に載せるアプリ」ではなく
「世界の地理・物流ネットワークをデータから再構成し、What-if シミュレーションできる
地球規模のデジタルモデル」** —— を、この実装の唯一の目的とする。

したがって本プロジェクトの成否は「地球儀が回るか」では測らない。**測るのは次の一点である。**

> 画面に出ている線・数・色のひとつひとつについて、
> それが**観測されたもの**なのか、**統計から来たもの**なのか、
> **こちらが推定したもの**なのか、**仮定した数式の出力**なのかを、
> 利用者が画面上で区別できること。

---

## 2. 機能要求

| ID | 要求 | 優先度 | 原典 |
|---|---|---|---|
| F-01 | 国・領域のポリゴンを地球儀に描く(Natural Earth 10m Admin 0) | must | §10, §45 |
| F-02 | EEZ のポリゴンを描く。**200NM / 重複主張 / 共同管轄を区別して描く** | must | §12, §45 |
| F-03 | 港を点として描く(World Bank Global International Ports) | must | §17, §45 |
| F-04 | チョークポイント 5 か所を印として描く | must | §31, §32 |
| F-05 | 港と港を結ぶ**推定航路**を描く。confidence は INFERRED を既定とする | must | §25, §28 |
| F-06 | レイヤー管理: ON/OFF・不透明度・Z 順・プリセット・検索・排他モード | must | §44–§48, §141 |
| F-07 | レイヤーを選ぶと出典・版・ライセンス・取得日・confidence を出す | must | §75, §76, §123 |
| F-08 | チョークポイントを 1〜90 日・容量減 0〜100% で閉じるシナリオを組める | must | §51–§54 |
| F-09 | 閉鎖時、影響を受けた航路を**その場で**再探索し、追加距離・追加日数を出す | must | §57–§60, §144 |
| F-10 | 再ルートで寄る荷を港ごとに積み、混雑度と 4 段階の区分を出す | must | §61–§63, §145 |
| F-11 | Day 0〜90 のタイムラインで閉鎖・再ルート・混雑の推移を見る | must | §55, §99 |
| F-12 | 影響指標(影響航路数・追加距離・追加日数・混雑増・レジリエンス)を出す | must | §100, §101 |
| F-13 | 画面の状態(レイヤー・不透明度・カメラ)を localStorage に保存する | should | §122 |
| F-14 | 出典一覧を About 画面に出す(6 出典の帰属表示) | must | §77 |
| F-15 | 領海(12NM)レイヤーは**取得できていないことを画面上で明示する** | must | §9 の差分 D-01 |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | ブラウザから外部データ API を一切呼ばない(Static-first) | 出荷 JS に外部ホストへの fetch が無いことを走査(G-11) |
| N-02 | 課金経路ゼロ。サーバー関数・DB・認証を持たない | リポジトリに `api/` `middleware` が無いことを走査 |
| N-03 | `public/data` の合計 < 30 MB、単一ファイル < 80 MB | `etl/validate/size.py`(G-02) |
| N-04 | 初期表示 < 5 秒(目標 3 秒)、レイヤー切替 < 200 ms、シミュレーション < 2 秒 | 実ブラウザ検品で実測(G-10) |
| N-05 | 同一 raw + 同一 ETL 版 → 同一出力(再現性) | SHA-256 をマニフェストに残し再実行で照合(G-13) |
| N-06 | 日本語本文に別字種・制御文字を混入させない | `python harness/text_hygiene.py`(G-09) |

## 4. データモデルの意味区分(原典 §3・§88 — **本仕様の背骨**)

出荷するすべての数と線は、次の 6 つのいずれか**ちょうど一つ**を名乗る。

| confidence | 意味 | 本プロジェクトでの実例 |
|---|---|---|
| `OBSERVED` | 実観測・公的地理データ | 国境ポリゴン・EEZ ポリゴン・港の座標 |
| `STATISTICAL` | 統計的集計 | 港の `outflows`(World Bank による集計値) |
| `INFERRED` | 複数データからの推定 | 航路(港 + 航行可能海域 + チョークポイント から導いた経路) |
| `ESTIMATED` | 数式・仮定による推定 | 所要日数(距離 ÷ 16 ノット) |
| `SIMULATED` | What-if 計算 | 閉鎖時の再ルート・追加距離・混雑度 |
| `PREDICTED` | ML モデル出力 | **本バージョンでは出荷しない**(§9 D-04) |

**絶対に混同しない対**(原典 §3.2):

```
TRADE_FLOW ≠ SHIP_TRAJECTORY
SHIPPING_DENSITY ≠ LIVE_AIS
INFERRED_ROUTE ≠ OBSERVED_ROUTE
SIMULATION ≠ PREDICTION
null ≠ 0
```

**`null` は 0 として描かない。** 値が無い欄は「—」と出し、0 の目盛りに点を打たない。

---

## 5. データ源(実測 2026-09-07)

| ID | 出典 | 取得 | 実測 |
|---|---|---|---|
| SRC-001 | Natural Earth Admin 0 Countries 10m v5.1.1 | `naciscdn.org` | 4,930,492 B / 258 行 / ADM0_A3 重複 0 |
| SRC-002 | Marine Regions World EEZ v12 (2023-10-25) LR | Zenodo 16355917 | 26,358,854 B / MD5 一致 / 285 行 |
| SRC-003 | Marine Regions World 12NM v4 | **取得不能**(§9 D-01) | — |
| SRC-004 | World Bank Global International Ports | datacatalogfiles | 236,236 B / 856 地物 / LOCODE 837 種 |
| SRC-005 | World Bank Global Shipping Traffic Density | **本バージョンでは取得しない**(§9 D-02) | 458 MB |
| SRC-006 | UNCTADstat | 未取得(§9 D-03) | HEAD が 403 |
| SRC-007 | UN Comtrade public preview | 到達確認済(鍵不要) | 応答 959 B |
| CHOKE | チョークポイント 5 か所 | 本リポジトリが持つ台帳 | 原典 §32 を写す |

**SRC-001 の直リンクは原典の記載どおりには動かない。** 原典 §10 が示す
`naturalearthdata.com/http//...` は HTTP 500 を返す(2026-09-07 実測)。
CDN 直(`naciscdn.org/naturalearth/10m/cultural/...`)から取得し、
zip 内の `VERSION.txt` が `5.1.1` であることで版を確かめる。

**SRC-002 は MD5 で検算できる。** Zenodo が公開する `1f3903992291fa90aa35bee579b456b5` と
ダウンロード実体が一致することを確認済み。これは**出典が自分で公開した値との照合**なので
循環していない。

**SRC-004 は自分が名乗る形式を守っていない**(HC-200)。

- 拡張子は `.geojson` だが **UTF-8 ではない**(cp1252。`Bahía Blanca` の `í` が 0xED)
- JSON の閉じ括弧の**後ろに 1 行** `System.IO.MemoryStream` が付いている
- 原典 §18/§135 が主キーに指定する UN/LOCODE が**一意でない**(856 件 / 837 種)

読み取り側にこの三つを**述語として置き**、崩れたら例外で止める(§7 の G-04)。

---

## 6. アーキテクチャ

```
公開データ → Python ETL → public/data(静的 JSON/GeoJSON)→ Vite ビルド → Vercel → ブラウザ
                                                                              ↓
                                                          ブラウザ内で航行グラフを組み Dijkstra
```

**シミュレーションはブラウザの中で走る。** サーバー関数を持たないので、
航行可能海域のグリッドと港・チョークポイントを配り、ブラウザが毎回グラフを組んで経路を引く。

### 6.1 航行グラフ(本実装の中核 — 原典 §25・§26 の具体化)

原典は航路を「港 + 海運密度 + 海洋地理 + チョークポイント + 既知の回廊」から構成せよと言う。
本実装はこれを**航行可能セルのグリッド上の最短経路**として実現する。

```
緯度経度グリッド(0.5 度)
  → セルを 5×5 に細分し、海である小点の割合が 0.5 以上のセルを航行可能とする
  → グリッドが塞いでしまう実在の海峡は、名前つきの通路として明示的に開ける
  → 運河は地峡を貫く明示の辺として足す        … CHOKE_SUEZ / CHOKE_PANAMA
  → 海峡はゲート円として持つ                  … CHOKE_MALACCA / HORMUZ / BAB_EL_MANDEB
  → 港をいちばん近い航行可能セルへ吸着させる
  → 8 近傍・測地線長を重みとする Dijkstra
```

- **運河型**(CANAL)の閉鎖 = その辺を落とす
- **海峡型**(STRAIT)の閉鎖 = ゲート円に入るセルを航行不能にする
- **容量減 p%** = その辺・そのセルの重みを `1/(1-p/100)` 倍する(100% は落とす)

### 6.1.1 解像度とマスクの作り方は測って決めた(2026-09-07 実測)

`scripts/calibrate_grid.py` で 5 通りを作り、**14 の海峡**(通れてほしい)と
**6 の地峡**(通れてほしくない)を、それぞれ**海峡を囲む枠の中だけの最短経路**で測った。

| 作り方 | 航行可能セル | packed | 塞がった海峡 | 漏れた地峡 |
|---|---|---|---|---|
| 中心点判定 @0.5° | 173,489 | 32,400 B | BOSPHORUS, GIBRALTAR | **PANAMA** |
| 中心点判定 @0.25° | 693,872 | 129,600 B | BOSPHORUS | — |
| 面積割合 ≥0.4 @0.5° | 174,999 | 32,400 B | BOSPHORUS | **PANAMA** |
| **面積割合 ≥0.5 @0.5°(採用)** | **173,574** | **32,400 B** | BOSPHORUS, GIBRALTAR | — |
| 面積割合 ≥0.6 @0.5° | 172,811 | 32,400 B | BOSPHORUS, GIBRALTAR | — |

**素朴な中心点判定はパナマ地峡に穴を開ける。** 0.5 度では地峡の両側が 267 km で
繋がってしまい、**運河を閉じても船が無料で太平洋へ抜ける**。面積割合 0.5 はこれを塞ぐ。

代償として狭い海峡が 2 つ塞がるので、**名前つきの通路**として明示的に開ける
(`etl/transform/passages.py`)。通路は数えられる形で残し、テストが「登録された通路が
実際に必要であること」と「登録されていない場所に穴が無いこと」の両方を確かめる。

**この測り方自体を一度間違えた。** 最初は「両端が同じ連結成分か」で測ったが、
全海洋がほぼ一つの成分になる(0.5 度で最大成分が航行可能セルの 99% 超)ので、
地球の裏側を回っても True になる。**枠内の局所最短経路**に直して初めて意味を持った。

### 6.2 なぜ「経路そのもの」を配るのではなくグリッドを配るのか

閉鎖のたびに経路が変わるので、事前計算した経路だけを配ると
**利用者が選んだシナリオに対しては何も答えられない**。グリッドはビット列なので小さく、
そこから経路を引く手続きは決定論的なので、Python 側と TypeScript 側で**同じ答え**が出る。
この一致が本プロジェクトのオラクルになる(G-06)。

---

## 7. 品質ゲート

| ID | ゲート | 判定 |
|---|---|---|
| G-01 | 出荷する全 GeoJSON の geometry が valid、座標が `[-180,180]×[-90,90]` に収まる | ETL 検証 + pytest |
| G-02 | `public/data` 合計 < 30 MB、単一ファイル < 80 MB | ETL 検証 |
| G-03 | 参照整合: 航路の port_id / chokepoint id、layers の data_source がすべて実在 | pytest |
| G-04 | 主キー一意性: `port_id` が一意。LOCODE 衝突は**運搬する全フィールドが一致する場合に限り**畳み、一致しなければ例外で止める。畳んだ件数をマニフェストに残す(HC-200) | pytest(陽性対照つき) |
| G-05 | 意味の非混同: 全成果物の `confidence` が §4 の enum に収まり、`layers.json` の宣言と成果物の実値が一致する | pytest |
| G-06 | 二実装照合: 同じグリッド・同じ始終点に対し、Python と TypeScript の Dijkstra が**距離だけでなく通過セル列まで**一致する。経路だけをずらした変異体を落とすことを陽性対照で示す(HC-065) | pytest + vitest(共有 golden) |
| G-07 | 経路の下界: 推定航路の距離 ≥ 両端の測地線距離。**この不等式は経路探索の実装を一切使わずに測る**ので循環しない | pytest |
| G-08 | 閉鎖の単調性: チョークポイントを閉じたとき、それを通っていた経路の距離は**増えるか到達不能になる**。短くなったら落とす | pytest + vitest |
| G-09 | 字種検査: `python harness/text_hygiene.py` が違反 0 | 検証段 |
| G-10 | 実ブラウザ検品: 複数画面幅で描画し、幾何(重なり・包含・可視域)と操作の到達を測る | Playwright |
| G-11 | 出荷ビルド: `npm run build` が通り、出力に外部ホストへの fetch が無い | 検証段 |
| G-12 | フッタ規約 5 項目(MIT / GitHub / 歩き方 / 設計図 / App Menu)が DOM に出る | Playwright |
| G-13 | 再現性: raw の SHA-256 と成果物の SHA-256 をマニフェストに残し、再実行で一致する | ETL 検証 |
| G-14 | 海峡の通過性: 登録した 15 の海峡が、**海峡を囲む枠の中だけを歩いて**測地線距離の 3 倍以内で通れる | pytest |
| G-15 | 地峡の非通過性: 登録した 6 の地峡は枠の中で**通れない**。あわせて運河の端点セルが 8 近傍で隣り合わないことを確かめる —— 隣り合えば運河を閉じても船が抜ける(HC-203) | pytest |
| G-16 | 台帳の緩みすぎ防止: 登録した通路は**実際に閉じていた場所を開けた**ものだけであること。何も開けない通路は例外で止める。通路の総数が 4 を超えたら較正のやり直しとする | pytest |

**G-xx はいずれも TEST_SPEC.md のケースから ID で参照されること**(HC-157)。
参照の無いゲートは本ファイルに「未実装」と明記する。

---

## 8. 画面

```
┌──────────────────────────────────────────────┬───────────────────┐
│                                              │ LAYER MANAGER     │
│                 3D GLOBE                     │  [search      ]   │
│                                              │  [ ] Exclusive    │
│         国 / EEZ / 港 / 航路 / チョーク        │  BASE             │
│                                              │  INFRASTRUCTURE   │
│                                              │  SIMULATION       │
├──────────────────────────────────────────────┤                   │
│ CHOKEPOINT SIMULATOR   Day ────●──── 90      │ DATA SOURCE       │
└──────────────────────────────────────────────┴───────────────────┘
```

レイヤーの並び(既定 Z 順、原典 §94):
Ocean → Country → Territorial Sea → EEZ → Shipping Density → Shipping Routes →
Trade Flow → Ports → Chokepoints → Simulation → Labels

---

## 9. 原典との差分(**すべてここに書く**)

| ID | 原典 | 本実装 | 理由 |
|---|---|---|---|
| D-01 | §14 領海 12NM を必須レイヤーとする | **出荷しない。画面には「未取得」と理由を出す**(F-15) | Marine Regions の配布が氏名・所属・メールアドレスの入力と同意チェックを要する Web フォームで、機械取得できない(2026-09-07 実測)。**利用者本人の個人情報を代わりに入力することはしない。** 12NM を海岸線からの緩衝で作れば数字は出るが、それは `OBSERVED` ではなく `INFERRED` であり、原典 §46 が要求する confidence と食い違う。**出さない方が正しい** |
| D-02 | §21 海運密度 458 MB を取得し 0.05° へ集約 | **本バージョンでは取得しない** | 458 MB のラスタは取得・集約とも本ループ群の予算に収まらない。レイヤー定義(`layers.json`)には枠として残し、`available: false` と理由を持たせる |
| D-03 | §33–§37 UNCTAD 統計 | 未取得。schema と枠のみ | `unctadstat.unctad.org` が HEAD に 403 を返す(2026-09-07)。取得経路の確立を後続ループへ回す |
| D-04 | §64–§67, §146 XGBoost による混雑予測 | **出荷しない** | 学習に使える実測が無い。手元にあるのは自分のシミュレータの出力だけで、それで学習した予測器は**シミュレータの再現以上のことを言わない**(循環)。原典 §3.2 が禁じる `SIMULATION ≠ PREDICTION` を、AI の側から破ることになる。代わりに**決定論的な感度分析**(容量減・期間を振って指標の応答を出す)を F-12 の中で提供する |
| D-05 | §10 Natural Earth 直リンク | `naciscdn.org` から取得 | 原典の URL が HTTP 500(2026-09-07 実測) |
| D-06 | §18/§135 `port_id = PORT_<UNLOCODE>` | 同じ規則を使うが、**衝突検査を通した上で**採用 | LOCODE は出典で一意でない(856 / 837)。HC-200 |
| D-07 | §7.1 React + Vite | 採用(フリートの既定は Next.js だが原典に従う) | 原典 §7.1 の明示 |
| D-08 | §57 Dijkstra | 採用。A\*・Yen's K は将来 | 原典 §57 の V3.2 指定どおり |

**D-01・D-02・D-04 は「作れなかった」ではなく「作らないと決めた」ものである。**
画面上でも、その旨を利用者に見せる(F-15 / §46 の `available` フラグ)。

---

## 10. スコープ外(原典 §148 に加えて)

```
Live AIS / Live ADS-B / リアルタイム船舶・航空機追跡
有償の海事・気象・地図 API
データベースサーバー / 認証 / ユーザーアカウント / リアルタイム多人数
航空ネットワーク(AIR_ROUTE)   … 原典 §102 でも必須外
資源レイヤー(§103)            … schema のみ
GNN / PyTorch Geometric(§68, §109)
```

さらに本バージョンでは §9 の D-01〜D-04 を出荷しない。

---

## 11. ID 規約(原典 §135)

```
COUNTRY     : ISO3(ADM0_A3)
PORT        : PORT_<UNLOCODE>        … 一意性を G-04 で保証
ROUTE       : ROUTE_<SOURCE>_<TARGET>
CHOKEPOINT  : CHOKE_<CODE>
LAYER       : UPPER_SNAKE_CASE
SCENARIO    : SCN_<TARGET>_<DURATION>D
```

## 12. ループ計画

| ループ | 範囲 | 主な成果物 |
|---|---|---|
| loop_001 | 足場・環境・データ源の実測 | SPEC / TEST_SPEC / venv / raw + manifest |
| loop_002 | 地理 ETL と航行グリッド | countries.geojson / eez.geojson / nav_grid |
| loop_003 | 港とチョークポイント | ports.geojson / chokepoints.json |
| loop_004 | Python 側の経路探索と golden | routes / golden fixtures / G-06,G-07,G-08 |
| loop_005 | 地球儀描画とレイヤー管理 | Vite + globe.gl / Layer Manager |
| loop_006 | TS 経路探索と二実装照合・閉鎖 UI | シミュレータ / タイムライン |
| loop_007 | 混雑・指標・レジリエンス | 指標パネル |
| loop_008 | 検品・フッタ・README・デプロイ | 本番 |
